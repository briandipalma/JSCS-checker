{
  "name": "jscs-checker",
  "version": "0.0.0",
  "scripts": {
    "start": "npm run build && node index.js",
    "build": "traceur --dir src/ out/ --modules=commonjs --sourcemap"
  },
  "dependencies": {
    "fell": "0.0.2",
    "bluebird": "^1.2.4",
    "jscs": "^1.4.0",
    "traceur": "0.0.41"
  }
}
